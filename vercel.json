{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "^/producers/?$", "dest": "/producers.json" },
      { "src": "/(.*)", "dest": "/index.html" }
    ]
}
